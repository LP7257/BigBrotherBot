package TacticusDataHandling;

/**
 * Contains the two different damage types, since we only want Battles to be in the final output, we need to sort out bombs.
 */
public enum DamageType {
        Battle,
        Bomb
}
